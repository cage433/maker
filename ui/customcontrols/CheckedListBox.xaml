<UserControl x:Class="com.trafigura.ui.customcontrols.CheckedListBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="ControlName" Height="295" Width="196">
    <Grid>
        <Grid.Resources>
            <Style x:Key="CheckBoxListItemStyle" TargetType="{x:Type ListBoxItem}">
                <Setter Property="Margin" Value="2"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                            <CheckBox Focusable="False" 
                           IsChecked="{Binding Path=IsSelected, 
                              Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent} }">
                                <ContentPresenter></ContentPresenter>
                            </CheckBox>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <ListBox x:Name="innerListBox" SelectionMode="Multiple"
                 ItemContainerStyle="{StaticResource CheckBoxListItemStyle}"
                 ItemsSource="{Binding ElementName=ControlName, Path=ItemsSource}">
        </ListBox>
    </Grid>
</UserControl>
