/**
 * WARNING: This file was automatically generated by the Trading Hub tooling version 2.15.
 * Any manual changes may be overwritten and lost!
 */


// Generated from model_service_http.rb

package com.trafigura.edm.logistics.inventory

import com.trafigura.edm.logistics.inventory._

import org.codehaus.jettison.json.JSONObject
import com.trafigura.tradinghub.support._
import javax.ws.rs._
import javax.ws.rs.core.Response
import org.jboss.resteasy.client.ClientResponse
import org.jboss.resteasy.annotations._
import scala.collection.JavaConversions._

// Some of the objects from the system namespace responses are
// used in handwritten code, so this hybrid resolution is required.
import com.trafigura.responses._


// JAX-RS support for EdmInventoryService


// A resource trait corresponding to EdmInventoryService

@Path("/EdmInventoryService")
trait EdmInventoryServiceResource {
  
  @Path("GetInventoryById")
  @POST @Consumes(Array("application/json")) @Produces(Array("application/json"))
  def getInventoryById(@HeaderParam("User") _userHeader:String, in: String): Response
  
  @Path("GetInventoryTreeByPurchaseQuotaId")
  @POST @Consumes(Array("application/json")) @Produces(Array("application/json"))
  def getInventoryTreeByPurchaseQuotaId(@HeaderParam("User") _userHeader:String, in: String): Response
  
  @Path("GetInventoryByGroupCompanyId")
  @POST @Consumes(Array("application/json")) @Produces(Array("application/json"))
  def getInventoryByGroupCompanyId(@HeaderParam("User") _userHeader:String, in: String): Response
  
}


// Wrap a EdmInventoryServiceResource (provided by RESTEasy's client
// support) as a EdmInventoryService
//
// Note: will throw an exception on parse/status code errors, exception will contain details of the error
//
class EdmInventoryServiceResourceProxy(val resource: EdmInventoryServiceResource) extends EdmInventoryService {

  val log = org.slf4j.LoggerFactory.getLogger(this.getClass.getName)

  
  def getInventoryById(inventoryId: Int) = getInventoryById(inventoryId,null)
  def getInventoryById(inventoryId: Int, securityContext:SecurityContext = null): com.trafigura.edm.logistics.inventory.LogisticsInventoryResponse = {
    val _userHeader = if (securityContext != null) { securityContext.user } else { null }
    val response = resource.getInventoryById(_userHeader, EdmInventoryServiceJSON.serializeGetInventoryById(inventoryId).toString)
    val content = response.asInstanceOf[ClientResponse[String]].getEntity(classOf[String])

    log.debug("Completed call to getInventoryById: " + response.getStatus() + " - " + content)

    if (response.getStatus() == Response.Status.OK.getStatusCode()) {
      
	      try {
          val obj = JSONConversions.parseJSON(content)
          val cache = new DeserialisationHelper
          (obj) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => cache.objectFor(o).getOrElse(cache.seen(o, com.trafigura.edm.logistics.inventory.LogisticsInventoryResponse.fromJson(o, cache))) }
	      } catch {
          case ex : Exception => {
            log.error("Parse exception from Status-Code: " + response.getStatus() + " on content: " + content + "; throwing exception...", ex)
            throw new Exception("Service invocation, response parse exception: Status Code: " + response.getStatus() + ", Content: '" + content + "'")
          }
        }
      
    }
    else { // non OK 200 status response
      log.debug("status NOT OK 200, throwing exception...")

      val obj = JSONConversions.parseJSON(content)

      val err = (obj) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => Result.fromJson(o, new DeserialisationHelper) }
        err match {
          case er : Result => {
            log.error("error response: " + er.message  + " { " + er.toString + " } status: " + response.getStatus)
            val ex = new Exception(er.message  + " { " + er.toString + " } status: " + response.getStatus)
            //ex.setStackTrace(er.stackTrace) // todo, need to add server stack trace, probably to customer application exception class
            throw ex
          }
          case _ => {
            log.error("unknown response, not an error response, content: " + content)
            throw new Exception("Unhandled response received, content: " + content)
          }
        }
    }
  }
  
  def getInventoryTreeByPurchaseQuotaId(quotaId: String) = getInventoryTreeByPurchaseQuotaId(quotaId,null)
  def getInventoryTreeByPurchaseQuotaId(quotaId: String, securityContext:SecurityContext = null): com.trafigura.edm.logistics.inventory.LogisticsInventoryResponse = {
    val _userHeader = if (securityContext != null) { securityContext.user } else { null }
    val response = resource.getInventoryTreeByPurchaseQuotaId(_userHeader, EdmInventoryServiceJSON.serializeGetInventoryTreeByPurchaseQuotaId(quotaId).toString)
    val content = response.asInstanceOf[ClientResponse[String]].getEntity(classOf[String])

    log.debug("Completed call to getInventoryTreeByPurchaseQuotaId: " + response.getStatus() + " - " + content)

    if (response.getStatus() == Response.Status.OK.getStatusCode()) {
      
	      try {
          val obj = JSONConversions.parseJSON(content)
          val cache = new DeserialisationHelper
          (obj) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => cache.objectFor(o).getOrElse(cache.seen(o, com.trafigura.edm.logistics.inventory.LogisticsInventoryResponse.fromJson(o, cache))) }
	      } catch {
          case ex : Exception => {
            log.error("Parse exception from Status-Code: " + response.getStatus() + " on content: " + content + "; throwing exception...", ex)
            throw new Exception("Service invocation, response parse exception: Status Code: " + response.getStatus() + ", Content: '" + content + "'")
          }
        }
      
    }
    else { // non OK 200 status response
      log.debug("status NOT OK 200, throwing exception...")

      val obj = JSONConversions.parseJSON(content)

      val err = (obj) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => Result.fromJson(o, new DeserialisationHelper) }
        err match {
          case er : Result => {
            log.error("error response: " + er.message  + " { " + er.toString + " } status: " + response.getStatus)
            val ex = new Exception(er.message  + " { " + er.toString + " } status: " + response.getStatus)
            //ex.setStackTrace(er.stackTrace) // todo, need to add server stack trace, probably to customer application exception class
            throw ex
          }
          case _ => {
            log.error("unknown response, not an error response, content: " + content)
            throw new Exception("Unhandled response received, content: " + content)
          }
        }
    }
  }
  
  def getInventoryByGroupCompanyId(groupCompanyMappingCode: String) = getInventoryByGroupCompanyId(groupCompanyMappingCode,null)
  def getInventoryByGroupCompanyId(groupCompanyMappingCode: String, securityContext:SecurityContext = null): com.trafigura.edm.logistics.inventory.LogisticsInventoryResponse = {
    val _userHeader = if (securityContext != null) { securityContext.user } else { null }
    val response = resource.getInventoryByGroupCompanyId(_userHeader, EdmInventoryServiceJSON.serializeGetInventoryByGroupCompanyId(groupCompanyMappingCode).toString)
    val content = response.asInstanceOf[ClientResponse[String]].getEntity(classOf[String])

    log.debug("Completed call to getInventoryByGroupCompanyId: " + response.getStatus() + " - " + content)

    if (response.getStatus() == Response.Status.OK.getStatusCode()) {
      
	      try {
          val obj = JSONConversions.parseJSON(content)
          val cache = new DeserialisationHelper
          (obj) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => cache.objectFor(o).getOrElse(cache.seen(o, com.trafigura.edm.logistics.inventory.LogisticsInventoryResponse.fromJson(o, cache))) }
	      } catch {
          case ex : Exception => {
            log.error("Parse exception from Status-Code: " + response.getStatus() + " on content: " + content + "; throwing exception...", ex)
            throw new Exception("Service invocation, response parse exception: Status Code: " + response.getStatus() + ", Content: '" + content + "'")
          }
        }
      
    }
    else { // non OK 200 status response
      log.debug("status NOT OK 200, throwing exception...")

      val obj = JSONConversions.parseJSON(content)

      val err = (obj) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => Result.fromJson(o, new DeserialisationHelper) }
        err match {
          case er : Result => {
            log.error("error response: " + er.message  + " { " + er.toString + " } status: " + response.getStatus)
            val ex = new Exception(er.message  + " { " + er.toString + " } status: " + response.getStatus)
            //ex.setStackTrace(er.stackTrace) // todo, need to add server stack trace, probably to customer application exception class
            throw ex
          }
          case _ => {
            log.error("unknown response, not an error response, content: " + content)
            throw new Exception("Unhandled response received, content: " + content)
          }
        }
    }
  }
  
}

// Wrap a EdmInventoryService as a EdmInventoryServiceResource (e.g. to be
// exposed by a JAX-RS implementation).

class EdmInventoryServiceResourceStub(val target: EdmInventoryService, filters: java.util.List[ServiceFilter])
    extends AbstractResourceStub(filters)
    with EdmInventoryServiceResource {

  def this(target: EdmInventoryService) = this(target, new java.util.ArrayList[ServiceFilter])

  requireFilters("com.trafigura.tradinghub.support.PermissionFilter","com.trafigura.services.security.ServiceOperationFilterBase")

  
  def getInventoryById(_userHeader:String, in: String): Response = {

    try {
    val params = new JSONObject(in)
     val res = 
      EdmInventoryServiceJSON.dispatch(target, filters.toSeq, "GetInventoryById", params, buildSecurityContext(_userHeader))

    
    Response.ok(JSONConversions.stringifyJSON(res.opt("result"))).build
    
    } catch {
      case ex:Throwable => generateErrorEntity(ex)
    }
  }
  
  def getInventoryTreeByPurchaseQuotaId(_userHeader:String, in: String): Response = {

    try {
    val params = new JSONObject(in)
     val res = 
      EdmInventoryServiceJSON.dispatch(target, filters.toSeq, "GetInventoryTreeByPurchaseQuotaId", params, buildSecurityContext(_userHeader))

    
    Response.ok(JSONConversions.stringifyJSON(res.opt("result"))).build
    
    } catch {
      case ex:Throwable => generateErrorEntity(ex)
    }
  }
  
  def getInventoryByGroupCompanyId(_userHeader:String, in: String): Response = {

    try {
    val params = new JSONObject(in)
     val res = 
      EdmInventoryServiceJSON.dispatch(target, filters.toSeq, "GetInventoryByGroupCompanyId", params, buildSecurityContext(_userHeader))

    
    Response.ok(JSONConversions.stringifyJSON(res.opt("result"))).build
    
    } catch {
      case ex:Throwable => generateErrorEntity(ex)
    }
  }
  
}
