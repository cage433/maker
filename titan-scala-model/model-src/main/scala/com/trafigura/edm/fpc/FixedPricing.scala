/**
 * WARNING: This file was automatically generated by the Trading Hub tooling version 2.15.
 * Any manual changes may be overwritten and lost!
 */


// Generated from model_object_scala.erb

package com.trafigura.edm.fpc





import com.trafigura.tradinghub.support._
import scala.collection.JavaConversions._



/**
 unfortunately this does not work(!) since when embedded in quota the bidirectional=true on specs causes a run-time error with hibernate, to be resolved...
define('PricingSpecification') {
 field 'pType',                     :enum, :enumerated => ['Floating', 'Fixed'] # indicate type of pricing used
 field 'specs',                    :list, :element_type => 'PricingSpec'
}

Fixed Pricing type of pricing specification
 */


 class FixedPricing extends com.trafigura.edm.fpc.PricingSpec  {
  


  





  override def equals(o: Any) = o match {
    case r: AnyRef =>
        if (o.isInstanceOf[com.trafigura.edm.fpc.FixedPricing]) {
            val that = r.asInstanceOf[com.trafigura.edm.fpc.FixedPricing]
            that.canEqual(this) && this.comments == that.comments && this.currencyId == that.currencyId && this.hedgeRequests == that.hedgeRequests && this.neptuneHedgeRequests == that.neptuneHedgeRequests && this.neptunePricingFixations == that.neptunePricingFixations && this.oid == that.oid && this.ordinal == that.ordinal && this.premium == that.premium && this.premiumComments == that.premiumComments && this.pricingFixations == that.pricingFixations && this.quantity == that.quantity &&  true
        }
        else false
    case _ => false
  }

  override def canEqual(that : Any) = that.isInstanceOf[com.trafigura.edm.fpc.FixedPricing]

  override def hashCode = {
    (41 * (41 * (41 * (41 * (41 * (41 * (41 * (41 * (41 * (41 * (41 *  1
    ) +
         (if (comments == null) 0 else comments.hashCode) 
    ) +
         currencyId.hashCode
        
    ) +
         (if (hedgeRequests == null) 0 else hedgeRequests.hashCode) 
    ) +
         (if (neptuneHedgeRequests == null) 0 else neptuneHedgeRequests.hashCode) 
    ) +
         (if (neptunePricingFixations == null) 0 else neptunePricingFixations.hashCode) 
    ) +
         oid.hashCode
        
    ) +
         ordinal.hashCode
        
    ) +
         (if (premium == null) 0 else premium.hashCode) 
    ) +
         (if (premiumComments == null) 0 else premiumComments.hashCode) 
    ) +
         (if (pricingFixations == null) 0 else pricingFixations.hashCode) 
    ) +
         (if (quantity == null) 0 else quantity.hashCode) 
    
  }

  override def toString = {
    "{com.trafigura.edm.fpc.FixedPricing: " + "" + "comments = " + comments + ", " + "currencyId = " + currencyId + ", " + "hedgeRequests = " + hedgeRequests + ", " + "neptuneHedgeRequests = " + neptuneHedgeRequests + ", " + "neptunePricingFixations = " + neptunePricingFixations + ", " + "oid = " + oid + ", " + "ordinal = " + ordinal + ", " + "premium = " + premium + ", " + "premiumComments = " + premiumComments + ", " + "pricingFixations = " + pricingFixations + ", " + "quantity = " + quantity +  "}"
  }


  
        override def toJson() : org.codehaus.jettison.json.JSONObject = {
      toJson(new SerialisationHelper)
    }

    override def toJson(differentiator: SerialisationHelper) : org.codehaus.jettison.json.JSONObject = {
        var result = super.toJson(differentiator);

        result.put("_node", differentiator.idFor(this))

        result.put("Type", "EDM.FPC.FixedPricing@1@1.0")
        
        return result;
    }

    override def loadJson(jobj : org.codehaus.jettison.json.JSONObject, cache: DeserialisationHelper) = {
        
            super.loadJson(jobj, cache);
        
        
          
        
    }

  
}

object FixedPricing {
  def version = ModelVersion("1")
  def wireFormatVersion = ModelVersion("1.0")

  
        val jsonTypeName = "EDM.FPC.FixedPricing"

    def fromJson(jobj : org.codehaus.jettison.json.JSONObject, cache: DeserialisationHelper = new DeserialisationHelper) : com.trafigura.edm.fpc.FixedPricing = {
      val v = TypeHolder(jobj.optString("Type"))

      v.assertModelVersion(version, "com.trafigura.edm.fpc.FixedPricing")
      v.assertWireFormatVersion(wireFormatVersion, "com.trafigura.edm.fpc.FixedPricing")

      
        
              v.typeName match {
              
                case _ =>
                  
                     val result = new com.trafigura.edm.fpc.FixedPricing()
                     result.loadJson(jobj, cache)
                     result
                  
            }
        
      
    }

  

  

  
    def apply() = {
      val res = new com.trafigura.edm.fpc.FixedPricing
      
      res
    }
  
}
