/**
 * WARNING: This file was automatically generated by the Trading Hub tooling version 2.15.
 * Any manual changes may be overwritten and lost!
 */


// Generated from model_service_json.rb

package com.trafigura.tradecapture.internal.refinedmetalreferencedataservice

import com.trafigura.tradecapture.internal.refinedmetal._

import org.codehaus.jettison.json.JSONObject
import com.trafigura.tradinghub.support._

// JSON support for GradeService

object GradeServiceJSON extends JSONServiceDispatcher[GradeService] {
  def serviceName = "TradeCapture.Internal.RefinedMetalReferenceDataService.GradeService"

  def decodeOperationName(jsonName:String) = {
    jsonName match {
      case "GetGrades" => "GetGrades"
      case "GetGradesForMetal" => "GetGradesForMetal"
      
    }
  }

  def parseParameters(operation:String, params: JSONObject) = {
    operation match {
      
      case "GetGrades" =>
        
          Map()
        
      
      case "GetGradesForMetal" =>
        
          val cache = new DeserialisationHelper
          Map("metal" -> ((params.opt("Metal")) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => cache.objectFor(o).getOrElse(cache.seen(o, com.trafigura.tradecapture.internal.refinedmetal.Metal.fromJson(o, cache))) }))
        
      
    }
  }

  def invoke(target: GradeService, operation: String, params: Map[String, Any]): Any = {
    operation match {
      
      case "GetGrades" =>
        target.getGrades()
      
      case "GetGradesForMetal" =>
        target.getGradesForMetal(params("metal").asInstanceOf[com.trafigura.tradecapture.internal.refinedmetal.Metal])
      
    }
  }

  def addResponse(operation:String, jsonResponse:JSONObject, response:Any) = {
    val differentiator = new SerialisationHelper
    operation match {
      
      case "GetGrades" =>
        jsonResponse.putOpt("result", new org.codehaus.jettison.json.JSONArray(if(response.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.Grade]] == null) new java.util.ArrayList() else java.util.Arrays.asList(response.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.Grade]].map(t => (t) match { case null => null; case o => o.toJson(differentiator) }).toArray: _*)))
      
      case "GetGradesForMetal" =>
        jsonResponse.putOpt("result", new org.codehaus.jettison.json.JSONArray(if(response.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.Grade]] == null) new java.util.ArrayList() else java.util.Arrays.asList(response.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.Grade]].map(t => (t) match { case null => null; case o => o.toJson(differentiator) }).toArray: _*)))
      
    }
  }

  
  def serializeGetGrades(): JSONObject = {
    val differentiator = new SerialisationHelper
    val params = new JSONObject
    
    params
  }
  
  def serializeGetGradesForMetal(metal: com.trafigura.tradecapture.internal.refinedmetal.Metal): JSONObject = {
    val differentiator = new SerialisationHelper
    val params = new JSONObject
    
      params.putOpt("Metal", (metal) match { case null => null; case o => o.toJson(differentiator) })
    
    params
  }
  

  class Proxy(val handler: (String, JSONObject) => JSONObject) extends GradeService {
  
    def getGrades(): List[com.trafigura.tradecapture.internal.refinedmetal.Grade] = {
        val request = new JSONObject
        request.put("method", "GetGrades")
        
        var routing = null
        val response = handler(routing, request)
        if (response == null) {
          null.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.Grade]]   // Doesn't work well with primitive types, but we should only see null in cases like discover anyway
        } else {
          val cache = new DeserialisationHelper
          JSONConversions.optionalList(response.opt("result"), x => (x) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => cache.objectFor(o).getOrElse(cache.seen(o, com.trafigura.tradecapture.internal.refinedmetal.Grade.fromJson(o, cache))) })
        }
    }
  
    def getGradesForMetal(metal: com.trafigura.tradecapture.internal.refinedmetal.Metal): List[com.trafigura.tradecapture.internal.refinedmetal.Grade] = {
        val request = new JSONObject
        request.put("method", "GetGradesForMetal")
        
        request.put("params", serializeGetGradesForMetal(metal))
        
        var routing = null
        val response = handler(routing, request)
        if (response == null) {
          null.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.Grade]]   // Doesn't work well with primitive types, but we should only see null in cases like discover anyway
        } else {
          val cache = new DeserialisationHelper
          JSONConversions.optionalList(response.opt("result"), x => (x) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => cache.objectFor(o).getOrElse(cache.seen(o, com.trafigura.tradecapture.internal.refinedmetal.Grade.fromJson(o, cache))) })
        }
    }
  
  }
}

