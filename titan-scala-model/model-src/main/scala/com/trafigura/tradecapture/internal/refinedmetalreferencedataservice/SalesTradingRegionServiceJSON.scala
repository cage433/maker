/**
 * WARNING: This file was automatically generated by the Trading Hub tooling version 2.15.
 * Any manual changes may be overwritten and lost!
 */


// Generated from model_service_json.rb

package com.trafigura.tradecapture.internal.refinedmetalreferencedataservice

import com.trafigura.tradecapture.internal.refinedmetal._

import org.codehaus.jettison.json.JSONObject
import com.trafigura.tradinghub.support._

// JSON support for SalesTradingRegionService

object SalesTradingRegionServiceJSON extends JSONServiceDispatcher[SalesTradingRegionService] {
  def serviceName = "TradeCapture.Internal.RefinedMetalReferenceDataService.SalesTradingRegionService"

  def decodeOperationName(jsonName:String) = {
    jsonName match {
      case "GetSalesTradingRegions" => "GetSalesTradingRegions"
      case "GetSalesTradingRegionsForLocation" => "GetSalesTradingRegionsForLocation"
      
    }
  }

  def parseParameters(operation:String, params: JSONObject) = {
    operation match {
      
      case "GetSalesTradingRegions" =>
        
          Map()
        
      
      case "GetSalesTradingRegionsForLocation" =>
        
          val cache = new DeserialisationHelper
          Map("location" -> ((params.opt("Location")) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => cache.objectFor(o).getOrElse(cache.seen(o, com.trafigura.tradecapture.internal.refinedmetal.DestinationLocation.fromJson(o, cache))) }))
        
      
    }
  }

  def invoke(target: SalesTradingRegionService, operation: String, params: Map[String, Any]): Any = {
    operation match {
      
      case "GetSalesTradingRegions" =>
        target.getSalesTradingRegions()
      
      case "GetSalesTradingRegionsForLocation" =>
        target.getSalesTradingRegionsForLocation(params("location").asInstanceOf[com.trafigura.tradecapture.internal.refinedmetal.DestinationLocation])
      
    }
  }

  def addResponse(operation:String, jsonResponse:JSONObject, response:Any) = {
    val differentiator = new SerialisationHelper
    operation match {
      
      case "GetSalesTradingRegions" =>
        jsonResponse.putOpt("result", new org.codehaus.jettison.json.JSONArray(if(response.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.SalesTradingRegion]] == null) new java.util.ArrayList() else java.util.Arrays.asList(response.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.SalesTradingRegion]].map(t => (t) match { case null => null; case o => o.toJson(differentiator) }).toArray: _*)))
      
      case "GetSalesTradingRegionsForLocation" =>
        jsonResponse.putOpt("result", new org.codehaus.jettison.json.JSONArray(if(response.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.SalesTradingRegion]] == null) new java.util.ArrayList() else java.util.Arrays.asList(response.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.SalesTradingRegion]].map(t => (t) match { case null => null; case o => o.toJson(differentiator) }).toArray: _*)))
      
    }
  }

  
  def serializeGetSalesTradingRegions(): JSONObject = {
    val differentiator = new SerialisationHelper
    val params = new JSONObject
    
    params
  }
  
  def serializeGetSalesTradingRegionsForLocation(location: com.trafigura.tradecapture.internal.refinedmetal.DestinationLocation): JSONObject = {
    val differentiator = new SerialisationHelper
    val params = new JSONObject
    
      params.putOpt("Location", (location) match { case null => null; case o => o.toJson(differentiator) })
    
    params
  }
  

  class Proxy(val handler: (String, JSONObject) => JSONObject) extends SalesTradingRegionService {
  
    def getSalesTradingRegions(): List[com.trafigura.tradecapture.internal.refinedmetal.SalesTradingRegion] = {
        val request = new JSONObject
        request.put("method", "GetSalesTradingRegions")
        
        var routing = null
        val response = handler(routing, request)
        if (response == null) {
          null.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.SalesTradingRegion]]   // Doesn't work well with primitive types, but we should only see null in cases like discover anyway
        } else {
          val cache = new DeserialisationHelper
          JSONConversions.optionalList(response.opt("result"), x => (x) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => cache.objectFor(o).getOrElse(cache.seen(o, com.trafigura.tradecapture.internal.refinedmetal.SalesTradingRegion.fromJson(o, cache))) })
        }
    }
  
    def getSalesTradingRegionsForLocation(location: com.trafigura.tradecapture.internal.refinedmetal.DestinationLocation): List[com.trafigura.tradecapture.internal.refinedmetal.SalesTradingRegion] = {
        val request = new JSONObject
        request.put("method", "GetSalesTradingRegionsForLocation")
        
        request.put("params", serializeGetSalesTradingRegionsForLocation(location))
        
        var routing = null
        val response = handler(routing, request)
        if (response == null) {
          null.asInstanceOf[List[com.trafigura.tradecapture.internal.refinedmetal.SalesTradingRegion]]   // Doesn't work well with primitive types, but we should only see null in cases like discover anyway
        } else {
          val cache = new DeserialisationHelper
          JSONConversions.optionalList(response.opt("result"), x => (x) match { case null => null; case o: org.codehaus.jettison.json.JSONObject => cache.objectFor(o).getOrElse(cache.seen(o, com.trafigura.tradecapture.internal.refinedmetal.SalesTradingRegion.fromJson(o, cache))) })
        }
    }
  
  }
}

