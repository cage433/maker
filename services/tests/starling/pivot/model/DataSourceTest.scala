package starling.pivot.model

import starling.utils.StarlingTest
import org.testng.annotations.{BeforeMethod, Test}
import org.testng.Assert._

/**
 * User: brian
 * Date: 01-Dec-2009
 * Copyright (c) Trafigura 2009
 */

class DataSourceTest extends StarlingTest {

//  val data = List(
//    Map(Field("Trade")->"T2", Field("Strike")->90),
//    Map(Field("Trade")->"T3", Field("Strike")->90)
//  )
//
//  @Test
//  def determineValuesFromPredicates {
//    val vals = new PivotResult(data, Map()).distinctValues(Field("Trade"), Map.empty + (Field("Strike") -> 90))
//    assertEquals(vals, List("T2", "T3"), "Got the wrong list from the predicates!")
//  }
//
//  @Test
//  def determineValuesFromPredicatesWithNoMatch {
//    val vals = new PivotResult(data, Map()).distinctValues(Field("Trade"), Map.empty + (Field("Strike") -> 110))
//    assertEquals(vals, Nil, "Got the wrong list from the predicates!")
//  }
}
