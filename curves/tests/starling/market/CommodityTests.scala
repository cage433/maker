package starling.market

import org.scalatest.matchers.ShouldMatchers
import starling.utils.ClosureUtil._
import starling.utils.StarlingSpec


class CommodityTests extends StarlingSpec with ShouldMatchers {
//  "All metals commodities should have a representative market" in {
//    Commodity.metalsCommodities.filterNot(_.hasRepresentativeMarket) should be === Nil
//  }
//
//  "All metals commodities should have a standard futures UOM" in {
//    Commodity.metalsCommodities.filter(metalCommodity => fails(metalCommodity.standardFuturesUOM)) should be === Nil
//  }
//
//  "commodity.standardFuturesUOM == commodity.representativeMarket.UOM" in {
//    Commodity.metalsCommodities.filter(mc => succeeds(mc.representativeMarket, mc.standardFuturesUOM))
//      .filterNot(mc => mc.representativeMarket.uom == mc.standardFuturesUOM) should be === Nil
//  }
}